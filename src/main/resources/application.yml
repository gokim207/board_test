spring:
  datasource:
    url: jdbc:mysql://dev-test-rds.crcgoowywj3v.ap-northeast-2.rds.amazonaws.com:3306/test
    username: admin
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 100MB
  cloud:
    aws:
      credentials:
        access-key: ${AWS_ACCESS_KEY_ID}
        secret-key: ${AWS_SECRET_ACCESS_KEY}
      s3:
        bucket: dev-test-uploaded-files
      region:
        static: ap-northeast-2
server:
  port: 80

jwt:
  secret-key: YXdzY3J1ZC1qd3Qtc2VjcmV0LWtleS1mb3ItZGV2LXVzZS1vbmx5LTEyMzQ1Njc4OTA=
  access-exp: 3600000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs
